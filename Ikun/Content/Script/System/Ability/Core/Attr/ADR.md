# 属性系统常见需求
1. 派生属性
    每1点力量增加5点生命上限和2点攻击力
2. 临时属性
    在GAS中通常不会直接修改Health而是修改一个IncomingDamage的隐藏属性
    当检测到IncomingDamage变化时: 触发护盾抵扣->减伤->无敌判断->扣除Health->IncomingDamage清零
# 2026/1/18:
- 属性管理器:
    加载配置表
    把配置表转化为高效的查找表
    把字符串的公式转化为函数
    构建属性关系的依赖图以及环路检测
- 属性集:
    存储属性值
    存储脏标记
    广播改动
# 2026/1/25:
- 整个技能系统, 包括其属性系统/技能系统/Buff系统的Tick应该像GAS一样由Actor驱动还是应该让其主控也就是技能系统单例来驱动?
    使用管理器驱动
- 属性集不挂载Tick, 全部使用事件驱动